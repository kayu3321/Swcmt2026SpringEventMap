rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /eventApplications/{document=**} {
      allow read, write: if false;
    }

    match /eventMapCongestionItems/{document=**} {
      allow read, write: if true;
    }
  }
}
